#!/usr/bin/env bash
# Glimpse Live Diagnostic & Model Verification Studio Launcher
set -e
SCRIPT_PATH="$(realpath "${BASH_SOURCE[0]}")"
GLIMPSE_ROOT="$(cd "$(dirname "${SCRIPT_PATH}")/.." && pwd)"
export PYTHONPATH="${GLIMPSE_ROOT}/src:${PYTHONPATH}"
export PYTHONUNBUFFERED=1
exec "${GLIMPSE_ROOT}/.venv/bin/python" "${GLIMPSE_ROOT}/src/glimpse/ui/diagnostics.py" "$@"
