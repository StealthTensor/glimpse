# Privacy

Glimpse is local-first.

Requirements: - no cloud inference; - no remote biometric upload; - no
analytics; - no raw frame retention by default; - minimal authentication
logs; - explicit user control over enrollment/removal.

Biometric data should be treated as sensitive even when technically
stored as embeddings.
