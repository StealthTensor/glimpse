# Application Protection

A visual lock overlay is not equivalent to OS-level protection.

Glimpse should distinguish: - UI convenience lock; - process/session
authentication; - OS authorization; - encrypted resource protection.

For serious protection, use Linux authorization mechanisms rather than
relying on a window that can potentially be bypassed.
